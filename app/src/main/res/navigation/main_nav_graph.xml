<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/contactListFragment">

    <fragment
        android:id="@+id/contactListFragment"
        android:name="com.example.contacts.ui.screens.ContactListFragment"
        android:label="fragment_contact_list"
        tools:layout="@layout/fragment_contact_list" >
        <action
            android:id="@+id/action_contactListFragment_to_newContactsScreen"
            app:destination="@id/newContactsScreen" />
    </fragment>
    <fragment
        android:id="@+id/newContactsScreen"
        android:name="com.example.contacts.ui.screens.NewContactsScreen"
        android:label="NewContactsScreen" />
</navigation>